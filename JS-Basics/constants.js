// Certain values should be fixed and cannot be changed

const interestRate = 0.3; // Makes it so that the variable cannot be reassigned
console.log(interestRate);

// const should be the default choice unless we need to change variables